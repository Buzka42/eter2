<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- The one network call the app makes: the bounded guidance payload, to
         the owner-controlled endpoint the build was compiled with. A build
         without ETER_AI_ENDPOINT never opens a socket, but the permission is
         declared unconditionally because it is the same binary. -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- Speaking a journal entry. The audio is handled by the platform's own
         recogniser and never reaches Eter as sound: only the transcript does,
         and only into the entry the person is already writing. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    <!-- The evening invitation, and nothing else. Deliberately stripped once,
         when Eter had no reason to interrupt anybody; it is back because
         DECISIONS.md grants exactly one quiet local notification, off by
         default, at the real sunset. There is no server behind it — nothing
         leaves the device to schedule a local alarm — and no other
         notification exists in the product to attach itself to this. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <!-- The invitation is placed at a computed sunset, so it has to survive a
         reboot; otherwise a phone restarted at lunchtime is silent that
         evening and there is no background poll to notice. -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION"/>
    <uses-permission android:name="android.permission.health.READ_ACTIVE_CALORIES_BURNED"/>
    <uses-permission android:name="android.permission.health.READ_STEPS"/>
    <uses-permission android:name="android.permission.health.READ_HEART_RATE"/>
    <uses-permission android:name="android.permission.health.READ_RESTING_HEART_RATE"/>
    <uses-permission android:name="android.permission.health.READ_HEART_RATE_VARIABILITY"/>
    <uses-permission android:name="android.permission.health.READ_RESPIRATORY_RATE"/>
    <uses-permission android:name="android.permission.health.READ_SLEEP"/>
    <!-- Workouts. Requesting the WORKOUT data type without this permission
         means Health Connect returns nothing and says nothing, which is how a
         phone full of Garmin runs reported zero sessions. -->
    <uses-permission android:name="android.permission.health.READ_EXERCISE"/>
    <!-- Writing back the two things Eter originates: a weight somebody typed
         and a confirmed meal. Nothing Eter *read* is ever written back — that
         would hand Health Connect its own data under a new name, and the next
         read would count it twice. See core/health/write_back.dart.

         Only these two, and deliberately no WRITE_STEPS, WRITE_EXERCISE or
         WRITE_SLEEP: those are things Eter receives, and there is no version of
         them it knows better than the watch does. -->
    <uses-permission android:name="android.permission.health.WRITE_WEIGHT"/>
    <uses-permission android:name="android.permission.health.WRITE_NUTRITION"/>
    <application
        android:label="Eter"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <intent-filter>
                <action android:name="androidx.health.ACTION_SHOW_PERMISSIONS_RATIONALE"/>
            </intent-filter>
        </activity>
        <activity-alias
            android:name="ViewPermissionUsageActivity"
            android:exported="true"
            android:targetActivity=".MainActivity"
            android:permission="android.permission.START_VIEW_PERMISSION_USAGE">
            <intent-filter>
                <action android:name="android.intent.action.VIEW_PERMISSION_USAGE"/>
                <category android:name="android.intent.category.HEALTH_PERMISSIONS"/>
            </intent-filter>
        </activity-alias>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <package android:name="com.google.android.apps.healthdata"/>
        <intent>
            <action android:name="androidx.health.ACTION_SHOW_PERMISSIONS_RATIONALE"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- Package visibility, Android 11 and later. Without these the app
             cannot see that a speech recogniser exists at all, `initialize()`
             returns false, and dictation reports itself unavailable on a phone
             that supports it perfectly well. -->
        <intent>
            <action android:name="android.speech.RecognitionService"/>
        </intent>
        <intent>
            <action android:name="android.speech.action.RECOGNIZE_SPEECH"/>
        </intent>
    </queries>
</manifest>
