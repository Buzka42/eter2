<?xml version="1.0" encoding="utf-8"?>
<!--
  Profile only, and identical to the debug copy. A profile build is AOT
  compiled, so it is the honest way to judge motion and art on a real phone,
  but it still needs to reach `tool/dev_endpoint.dart` on the development
  machine — over `adb reverse tcp:8787 tcp:8787`, which makes the host's port
  appear on the phone's own loopback.

  Release builds merge neither this file nor the debug one, and cannot reach a
  cleartext endpoint at all.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
